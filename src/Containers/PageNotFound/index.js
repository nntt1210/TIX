import { Box, Typography } from "@material-ui/core";
import React, { PureComponent } from "react";

export default class PageNotFound extends PureComponent {
  render() {
    return (
      <Box>
        <Typography component="h3">PageNotFound</Typography>
      </Box>
    );
  }
}
